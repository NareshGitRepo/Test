import { Routes } from '@angular/router';
import { SegmentManagementComponent } from './manage/segment-management.component';

export const segmentRoutes:Routes = [
    {
        path:'',
        component:SegmentManagementComponent
    }
]